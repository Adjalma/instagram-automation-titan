# Cron de comentários — executa a cada 2 minutos via GitHub Actions (gratuito)
# Chama o endpoint /api/cron/comments da Vercel com autenticação por CRON_SECRET

name: Cron — Responder Comentários

on:
  schedule:
    - cron: '*/2 * * * *'
  workflow_dispatch: # permite disparar manualmente

jobs:
  comments:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Chamar endpoint de comentários
        run: |
          curl -s -o response.json -w "%{http_code}" \
            -X GET \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.VERCEL_APP_URL }}/api/cron/comments" > http_code.txt
          HTTP_CODE=$(cat http_code.txt)
          echo "HTTP Status: $HTTP_CODE"
          cat response.json
          if [ "$HTTP_CODE" != "200" ]; then
            echo "Erro: HTTP $HTTP_CODE"
            exit 1
          fi
